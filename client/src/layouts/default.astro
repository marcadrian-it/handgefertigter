---
const navItems = [
	{
		path: '/einkauf',
		label: 'Einkauf',
	},
    {
        path: '/kontakt',
        label: 'Kontakt',
    },

	{
		path: '/ueber-mich',
		label: 'Über mich',
	},
];
import "../../styles/global.css";
const pathname = new URL(Astro.request.url).pathname;
---

<html lang="de">
	<head>
		<meta charset="utf-8" />
		<meta name="description" content="Handgefertigte Produkte" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Handgefertigter</title>
	</head>
	<body class="flex flex-col min-h-screen">
		<header class="flex align-baseline bg-crimson-100 flex-row justify-between p-5">

			<a href="/" rel="home" class="font-ysabeau font-bold text-4xl md:text-2xl xl:text-3xl">
				<span role="img" aria-label="garnrolle">🧵</span>
				Handgefertigter
			</a>


			<nav class="flex gap-4 items-center pr-4 text-2xl md:text-xl font-extrabold sm:hidden md:pr-1">
				{navItems.map((item) => {
					return (
					  <a
						class={`hover:underline decoration-2 hover:scale-105 ${
						item.path === pathname ? "scale-105 underline" : ""
						}`}
						href={item.path}
					  >
						{item.label}
					  </a>
					);
				  })}
			</nav>

		</header>
		<main>
		<slot />
	</main>
		<footer class="flex justify-center mt-auto p-6">
			<p>
				<a class="text-sm hover:underline" href="https://marcadrian.me" target="_blank">created by marcadrian©2023</a>
			</p>
		</footer>
	</body>
</html>
