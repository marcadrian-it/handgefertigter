---
const navItems = [
	{
		path: '/einkauf',
		label: 'Einkauf',
	},
    {
        path: '/kontakt',
        label: 'Kontakt',
    },

	{
		path: '/ueber-mich',
		label: 'Über mich',
	},
];
---

<html lang="de">
	<head>
		<meta charset="utf-8" />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link href="https://fonts.googleapis.com/css2?family=Ysabeau+SC:wght@700&display=swap" rel="stylesheet"> 

		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Handgefertigter</title>
	</head>
	<body>
		<header>
			<a href="/" rel="home">
				<span role="img" aria-label="garnrolle">🧵</span>
				Handgefertigter
			</a>

			<nav>
				{
					navItems.map((item) => {
						return <a href={item.path}>{item.label}</a>;
					})
				}
			</nav>
		</header>

		<slot />

		<footer>
			<p>
				<a href="https://marcadrian.me" target="_blank">created by marcadrian</a>
			</p>
		</footer>
	</body>
</html>

<style is:global>
	:root {
        --color-crimson-100: hsl(348, 83%, 90%);
        --color-crimson-200: hsl(348, 83%, 80%);
        --color-crimson-600: hsl(348, 83%, 40%);
        --color-crimson-700: hsl(348, 83%, 30%);
        --color-crimson-800: hsl(348, 83%, 20%);
	}


	* {
		box-sizing: border-box;
	}

	html {
		font-family: -apple-system, BlinkMacSystemFont, 'Gill Sans', Roboto,
			Helvetica, Arial, sans-serif, 'Apple Color Emoji', 'Gill Sans Emoji',
			'Gill Sans Symbol';
		font-size: 18px;
		line-height: 1.45;
	}

	body {
		margin: 0;
	}

	:is(h1, h2, h3, h4, h5, h6) {
		font-family: 'Ysabeau SC', sans-serif;
		font-weight: 700;
		line-height: 1.1;
		margin: 1.5rem 0 0;
	}

	.block {
		padding: 3rem max(5vw, calc((100% - 800px) / 2));
	}

	.big-link {

	}

	.big-link:hover {

	}

	.big-link:is(:active, :focus) {

	}
</style>

<style>
	header {
		align-items: baseline;
		background: var(--color-crimson-100);
		display: flex;
		flex-direction: column;
		justify-content: space-between;
		padding: 1rem;
	}

	a[rel='home'] {
		color: inherit;
		font-family: 'Ysabeau SC', sans-serif;
		font-size: 1.5rem;
		font-weight: 700;
		text-decoration: none;
	}

	nav {
		display: flex;
		gap: 1rem;
	}

	nav a {
		color: inherit;
		font-size: 1.25rem;
		font-weight: 700;
		text-decoration: none;
	}

	footer {
		font-size: 0.75rem;
		padding: 3rem 5% 2rem;
	}

	footer p {
		display: flex;
		flex-wrap: wrap;
		gap: 1.5rem;
		justify-content: center;
	}

	footer a {
		color: var(--color-crimson-600);
		display: block;
	}

	@media (min-width: 400px) {
		header {
			flex-direction: row;
		}
	}
</style>